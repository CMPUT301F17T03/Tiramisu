<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dizhang.com.example.tiramisu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:label="Tiramisu" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".HabitManagerActivity" />
        <activity android:name=".EventManagerActivity" />
        <activity android:name=".MapActivity" />
        <activity android:name=".ShareActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".HabitViewActivity" />
        <activity android:name=".EventViewActivity" />
        <activity android:name=".HabitNewActivity" />
        <activity android:name=".EditHabitActivity"></activity>
    </application>

</manifest>